{
  "name": "trustify-ui-tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "codegen": "npx playwright codegen http://localhost:8080/",
    "test": "npx bddgen && TRUSTIFY_URL=http://localhost:3000 npx playwright test --trace on --project=chromium --project chromium",
    "format:check": "prettier --check './**/*.{ts,js,json}'",
    "format:fix": "prettier --write './**/*.{ts,js,json}'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.0.2",
    "playwright-bdd": "^8.0.1",
    "prettier": "^3.3.3"
  }
}
